import styled from "styled-components";

const Container = styled.li``;

const Preferences = () => {
  return (
    <Container>
      <h2>Preferences</h2>
    </Container>
  );
};

export default Preferences;
