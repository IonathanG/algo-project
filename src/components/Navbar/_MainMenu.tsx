import styled from "styled-components";

const Container = styled.li``;

const MainMenu = () => {
  const menu = [];
  return (
    <Container>
      <h2>Main Menu</h2>
    </Container>
  );
};

export default MainMenu;
